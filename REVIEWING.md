# 审核与发布流程

本文件规定论文教程从 Pull Request 到公开发布的完整流程。自动检查负责工程门槛，论文内容和教学质量必须由人工审核。

## 1. 角色分工

- **作品作者**：生成、核查和修改教程，修复审核问题并提供证据；
- **工程审核者**：检查目录、构建、交互、响应式布局、资源路径和敏感文件；
- **内容审核者**：检查研究问题、方法、公式、实验数字、结论、局限和中文讲解；
- **维护者**：确认审核结论、发布状态和 CI 后合并，并检查 Pages 部署。

同一维护者可以承担多个角色，但作者不能用自我审核代替至少一位维护者的批准。

## 2. 状态定义

`paper.json` 的 `status` 使用以下状态：

| 状态 | 含义 | 是否可以合并到 `main` |
| --- | --- | --- |
| `draft` | 本地制作或尚未完成自检 | 否 |
| `review` | 已创建 PR，正在接受工程和内容审核 | 否 |
| `published` | 两类审核通过，可以合并并公开发布 | 是 |

导入脚本默认写入 `review`。工程审核和内容审核全部通过后，作者根据审核意见将状态改为 `published`，重新运行 `npm run catalog` 并推送到原 PR。维护者完成最终确认后合并。

## 3. 审核步骤

### 第一步：自动检查

PR 必须通过：

```text
npm run validate
npm run validate:pr
npm run catalog:check
npm run build:site
```

任何检查失败都应先修复，不进入最终审核。

### 第二步：工程审核

检查：

- 目录和 `paper.json` 符合规范；
- 项目可以安装、运行和构建；
- 主要交互、键盘操作和即时反馈正常；
- 桌面端与移动端不存在明显溢出、遮挡或资源丢失；
- 没有 `node_modules/`、`dist/`、密钥、隐私或本地绝对路径；
- 图片、数据和外部资源具有可追溯来源。

### 第三步：内容审核

对照原论文检查：

- 研究问题、动机和贡献；
- 方法结构、训练和推理流程；
- 关键公式、符号和术语；
- 实验数字、对比方法和消融结论；
- 局限、适用范围和没有被论文支持的推断；
- 中文讲解、类比和交互是否准确表达论文机制。

### 第四步：人工修改审核

根据一页修改说明确认：

- 至少三项实质性修改已经落实到项目；
- 每项修改包含问题、修改内容、原因和结果；
- 修改覆盖两个及以上类别；
- 作者能够解释修改与论文原文的关系。

### 第五步：反馈与复审

审核意见使用以下结论：

- **阻塞问题**：必须修改，否则不能发布；
- **建议问题**：建议改进，但不单独阻止发布；
- **已通过**：该审核维度无需继续修改。

作者应在原 PR 中逐项回复，并说明修改位置和验证方法。新增提交后，审核者重新检查受影响部分；新的实质性提交应使旧批准失效。

### 第六步：发布

合并前必须同时满足：

- CI 全部通过；
- 工程审核通过；
- 内容审核通过；
- `paper.json.status` 已改为 `published`；
- `catalog/papers.json` 已更新；
- 至少一位维护者批准 PR；
- 所有阻塞讨论已经解决。

合并后由 GitHub Pages 自动部署。维护者应打开集合页和教程页，确认入口、资源和主要交互正常。

## 4. 发布后问题

发布后发现事实错误、资源失效或隐私问题时，应立即创建修复 PR。严重问题可以先从目录中下线，再通过审核恢复；不要绕过审核直接修改 `main`。

## 5. 审核记录

审核结论保留在 Pull Request 中。评分记录和包含学生个人信息的材料保存在考核指定的私有渠道，不提交到公开仓库。
